# network-monitoring

## To run the app
1. Create a virtualenv, ```virtualenv venv``` and activate it ```source venv/bin/activate```
2. Install dependencies, ```pip install -r requirements.txt```
3. Run the app, ```sudo streamlit run dashboard.py```